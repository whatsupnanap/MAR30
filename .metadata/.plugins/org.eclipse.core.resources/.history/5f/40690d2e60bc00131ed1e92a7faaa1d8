var openWindow = function(){
	var newWindowj= Ti.UI.createWindow({
		backgroundColor: "blue"
	});
	newWindow.open();
	
};

var clickViews = Ti.UI.createButton({
	width: 10,
	height: 50,
	backgroundColor: "#ff0000"
});


button.addEventListener("click",openWindow);

var message = "can you see this?";


exports.msg-message;


win.add(clickViews);